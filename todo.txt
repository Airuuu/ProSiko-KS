TODO:

MODULES:
module offline handling <- important for main functionality
make every module offline during exit command <- server shut down

file module
grpc module

config service:
if using specified module, prevent disabling it <-forcing to let minimum 1 module available, so server wont be functionless (is it necessary? ASK!)

KNOWN BUGS:
Any module disconnect, then connect them back (except udp)

Questions:
Rozłączanie i Podłączanie listenerów - czy komunikatory też mają być wyłączane i włączane?
